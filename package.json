{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync:projects": "node scripts/sync-projects.mjs",
    "sync:projects:manifest": "node scripts/sync-projects.mjs --skip-images",
    "export:translations": "node scripts/export-translations.mjs",
    "import:translations": "node scripts/import-translations.mjs && npm run sync:projects:manifest",
    "build:app": "tsc && vite build",
    "dev": "npm run sync:projects && vite",
    "build": "npm run sync:projects && npm run build:app",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@fontsource/merriweather": "^5.3.0",
    "@fontsource/roboto": "^5.3.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5",
    "@vitejs/plugin-react": "^4.7.0",
    "sharp": "^0.35.4",
    "typescript": "~6.0.2",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@react-pdf/renderer": "^4.9.0",
    "i18next": "^26.4.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-i18next": "^17.0.13",
    "react-router-dom": "^7.18.3"
  }
}
